<div class="grey-box">
  <h2>Approver Photos Page</h2>

  <h3><%= @approver.first_name + " " + @approver.last_name %></h3>

  <div class="blue-box">
    <p>Here include all photos associated with this approver.</p>
  </div>

  <% @approver.photos.each do |photo| %>
    <h4><%= photo.title %></h4>
    <p><%= photo.caption %></p>

    <div id="edit-photo-form"></div>

    <%= link_to "Edit", edit_user_approver_photo_path(current_user, @approver, photo.id), remote: true, class: "btn btn-transparent btn-default" %>
    <%= link_to "Delete", user_approver_photo_path(current_user, @approver, photo.id), data: {confirm: 'Are you sure?'}, method: :delete, class: "btn btn-transparent btn-default" %>
  <% end %>

  <%= link_to "Add Photo", new_user_approver_photo_path(current_user, @approver.id), remote: true, class: "btn btn-transparent btn-default" %>

  <div id="new-photo-form"></div>

</div>
