<%= simple_form_for [current_user, @assignee, @reference] do |f| %>
  <%= f.hidden_field :assignee_id, value: @assignee.id %>
  <%= f.input :title %>
  <%= f.input :comments %>
  <%= f.input :document, as: :attachinary %>
  <%= f.button :submit %>
<% end %>
