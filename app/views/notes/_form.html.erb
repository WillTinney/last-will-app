<%= simple_form_for [current_user, @assignee, @note] do |f| %>
  <%= f.hidden_field :assignee_id, value: @assignee.id %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>
