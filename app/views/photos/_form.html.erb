<%= simple_form_for [current_user, @assignee, @photo] do |f| %>
  <%= f.hidden_field :assignee_id, value: params[:assignee_id] %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.input :title %>
  <%= f.input :caption %>
  <%= f.button :submit %>
<% end %>

